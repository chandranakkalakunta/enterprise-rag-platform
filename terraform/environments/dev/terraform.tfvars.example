# Copy to terraform.tfvars (gitignored) and adjust for local apply.
# Application code must not hard-code the project ID — use GCP_PROJECT_ID / var.gcp_project_id.
# Current dev project number (reference only): 642114828076

gcp_project_id = "enterprise-rag-platform-502711"
region         = "asia-south1"
environment    = "dev"

labels = {
  project     = "enterprise-rag-platform"
  managed_by  = "terraform"
  environment = "dev"
}
