# Enterprise RAG Platform — environment variable template
# Copy to .env for local development. Never commit real secrets.
# Production secrets live in Google Secret Manager.

# GCP (set via env; never hard-code in application source)
# Current dev example project number: 642114828076
GCP_PROJECT_ID=enterprise-rag-platform-502711
GCP_REGION=asia-south1

# API
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=INFO
ENVIRONMENT=local

# Frontend (public — safe for browser)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Auth (placeholders — wire in Phase 1)
# OAUTH_CLIENT_ID=
# OAUTH_CLIENT_SECRET=   # Secret Manager only in deploy

# Vertex AI (model IDs pinned per environment later)
# VERTEX_LOCATION=asia-south1
# GEMINI_MODEL=gemini-2.5-flash
# EMBEDDING_MODEL=
