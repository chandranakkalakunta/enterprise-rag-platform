# Enterprise RAG Platform — backend dependencies (pinned)
# CI must install ONLY from this file. Run `pip check` after install.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
httpx==0.28.1
python-multipart==0.0.20

# GCP clients (Phase 2.1 — upload → GCS + Firestore)
google-cloud-storage==2.19.0
google-cloud-firestore==2.19.0

# Google ID token verification (Phase 5.1 / ADR-0009)
google-auth==2.37.0

# Vertex AI embeddings + Gemini (Phase 3.1 / 3.4)
google-cloud-aiplatform==1.74.0

# Query orchestration (Phase 3.4 / ADR-0008)
langgraph==0.2.60
langchain-core==0.3.28

# Text extraction (Phase 2.2 — isolated for future ingest-worker move)
pdfminer.six==20250327

# Test
pytest==8.3.4
pytest-cov==6.0.0
pytest-asyncio==0.25.0
